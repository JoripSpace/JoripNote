{
  "name": "joripnote",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "node --check worker.js",
    "lint": "node --check worker.js && node --check tests/worker.test.mjs",
    "test": "node --test",
    "build": "node --check worker.js"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
